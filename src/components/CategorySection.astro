---
// src/components/CategorySection.astro
import SiteCard from './SiteCard.astro';
import { Icon } from 'astro-icon/components';

interface Props {
  category: string;
  subCategoryName?: string;
  subCategoryIcon?: string;
  sites: Array<{
    name: string;
    url: string;
    icon?: string;
    description?: string;
    // 其他站点属性...
  }>;
}

const { category, subCategoryName, subCategoryIcon, sites } = Astro.props;
---

<div class="category-section" id={`category-${category.toLowerCase().replace(/\s+/g, '-')}`}>
  <h4 class="text-lg mt-0 mb-4">
    {subCategoryIcon && <Icon name={subCategoryIcon} class="mr-1 icon-fw icon-lg" />}
    {subCategoryName && subCategoryName}
  </h4>
  <div class="flex flex-wrap -mx-[15px]">
    {sites.map((site) => (
      <SiteCard data={site} />
    ))}
  </div>
</div>
